version: '3'

services:
  proxy:
    image: nginx:latest
    container_name: proxy
    ports:
      - "80:80"   
    volumes:
      - /etc/nginx/conf.d:/etc/nginx/conf.d:ro
    networks:
      - myapp-network
    restart: always

networks:
  myapp-network:
    external: true
