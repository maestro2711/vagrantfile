version: '3'
services:
  nginx:
    image: {{ nginx_image }}
    ports:
      - "{{ proxy_port }}:{{ proxy_port }}"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    restart: always
    networks:
      - myapp-network

networks:
  myapp-network:
    external: true