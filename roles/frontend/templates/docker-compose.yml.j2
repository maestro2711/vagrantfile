version: '3'
services:
  frontend:
    image: {{ frontend_image }}
    container_name: frontend
    expose:
      - "3000"
    environment:
      - REACT_APP_API_URL=/imc
    restart: always
    networks:
      - myapp-network

networks:
  myapp-network:
    external: true
